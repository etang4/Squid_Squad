using UnityEngine;
using System.Collections;

public class CountDown : MonoBehaviour {

	/*private string counter = "";    
	private bool isCounting = false;

	// In this example we show how to invoke a coroutine and continue executing
	// the function in parallel.
	
	void Start() {
		// this is the same as WaitAndPrint(2.0) as the compiler does it for you automatically
		StartCoroutine(counting ()); 
		print ("Before WaitAndPrint Finishes " + Time.time);
	}
	// call this function to display countdown
	void counting ()    
	{
		isCounting = true;    
		
		counter = "3";    
		yield WaitForSeconds (1.5);  //makes system buffer waits
		
		counter = "2";    
		yield WaitForSeconds (1.5);
		
		counter = "1";    
		yield WaitForSeconds (1.5);
		
		counter = "GO";    
		yield WaitForSeconds (1.5);
		
		isCounting = false;
		counter = "";  //makes the string empty
	}

	void OnGUI ()
	{
		if (isCounting)
		{    
			GUI.color = Color.red;    
			GUI.Box (Rect (Screen.width / 2 - 100, 50, 200, 175), "GET READY");
 
			GUI.color = Color.white;    
			GUI.Box (Rect (Screen.width / 2 - 90, 75, 180, 140), counter);
		}    
	}*/
}
